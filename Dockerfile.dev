# 开发环境 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 安装 SQLite 依赖
RUN apk add --no-cache python3 make g++ sqlite

# 复制 package.json
COPY backend/package*.json ./

# 安装依赖（包含开发依赖）
RUN npm install

# 暴露端口
EXPOSE 3000

# 默认命令
CMD ["npm", "run", "dev"]
