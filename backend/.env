# 测试环境配置
NODE_ENV=development
PORT=3000

# 数据库配置（测试用）
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=wordheat

# JWT密钥
JWT_SECRET=test-secret-key-for-development-only

# 微信小程序（测试用）
WX_APPID=test-appid
WX_SECRET=test-secret
